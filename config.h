#pragma once

#include <SFML/Graphics.hpp>

namespace config {

	const int width = 1280;
	const int height = 720;

	const int numberOfMussels = 10;

	const sf::Color background = sf::Color(0, 230, 230);

}